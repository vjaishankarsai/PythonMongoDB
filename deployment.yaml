apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: py
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: py
    spec:
      containers:
      - name: py
        image: vjaishankarsai/pyimage:first
        ports:
        - containerPort: 80
      - name: mongo
        image: mongo
        ports:
        - containerPort: 27017
